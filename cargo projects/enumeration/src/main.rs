use enumeration::{advanced::*, basic::*, if_let::*, other_keyword::*};
fn main() {
    // basic();
    // advanced();
    // other_keyword();
    if_let(Some(4));
    which_face(Dice::Two);
    this_or_that(None);
}
